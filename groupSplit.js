function groupSplit (arr, size=arr.length) {
  if (!Array.isArray(arr) || size === 0 || arr.length === 0) {
    return
  }
  if (size === 1) {
    return arr.map(item => [item])
  }
  for(let i=0;i<arr.length;i++) {
  }
}

groupSplit();
